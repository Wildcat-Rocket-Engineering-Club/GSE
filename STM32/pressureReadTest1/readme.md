Added an (untested) program that reads analog channels 0, 1, 2 (corresponding to bottle, tank, chamber pressure) from 0 to 2.5 V for 0 to 5000 psi (voltage divider halves the voltage)

and outputs the data in json formatting with dummy values for stuff that isn't supported yet, like valve and load cell commands.